============
Installation
============

At the command line::

    $ pip install nova-solver-scheduler

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nova-solver-scheduler
    $ pip install nova-solver-scheduler
